機能拡張の余地:
  2. client 固定 → カスタマイズ可能に（プロジェクトによって api, httpClient など異なる）
  3. YAML形式のOpenAPI仕様に未対応
  4. 変数追跡が浅い（const path = getPath(); client.GET(path) は追跡できない）

  テストの不足:
  5. CLIのE2Eテスト（実際にコマンド実行して終了コード確認）
  6. エラーケース（不正なOpenAPI、存在しないパス等）

  DX:
  7. エラーメッセージが簡素（どこで問題が起きたか分かりにくい）
