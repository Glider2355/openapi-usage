pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,tsx,json}"
      run: pnpm biome check {staged_files}
    format:
      glob: "*.{js,ts,tsx,json}"
      run: pnpm biome format --write {staged_files} && git add {staged_files}
      stage_fixed: true
